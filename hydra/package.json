{
  "name": "hydra",
  "version": "0.1.0",
  "description": "Tacit sub-agent spawner",
  "type": "module",
  "bin": {
    "hydra": "./dist/hydra.js"
  },
  "scripts": {
    "build": "node --experimental-strip-types build.ts",
    "e2e:acceptance": "node --experimental-strip-types scripts/e2e-acceptance.ts",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "test:anti-optimistic": "node --experimental-strip-types --test tests/anti-optimistic.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  }
}
